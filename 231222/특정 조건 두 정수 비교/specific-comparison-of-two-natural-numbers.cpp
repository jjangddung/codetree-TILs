#include <iostream>

using namespace std;


int main() {
    int a, b;
    cin >> a >> b;

    cout << (a < b ? 1:0 );
    cout << " " ;
    cout << (a==b ? 1:0 ) ;

    // 여기에 코드를 작성해주세요.
    return 0;
}